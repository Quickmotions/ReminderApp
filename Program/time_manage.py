from datetime import datetime, timedelta


def update_reminder_times(initial_datetime, repeats):
    now = datetime.now()
    current_time = str(now.strftime("%H:%M"))
    current_date = str(now.strftime('%Y:%m:%d'))


def hourly(initial_datetime):


    return new_datetime


def daily(initial_datetime):
    return new_datetime


def monthly(initial_datetime):
    return new_datetime


def yearly(initial_datetime):
    return new_datetime


# now = datetime.now() + timedelta(hours=10)
# print(now.strftime('%H:%M'))
# print(now.strftime('%Y:%m:%d'))